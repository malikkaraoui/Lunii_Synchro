{
  "name": "synchro_boite_a_histoires",
  "version": "2.1.12",
  "description": "Synchro Boîte à histoires V2 — Transfert audio vers boîte à histoires",
  "scripts": {
    "dev":            "tauri dev",
    "build":          "tauri build",
    "build:mac-app-store": "tauri build --bundles app --target universal-apple-darwin --config src-tauri/tauri.appstore.conf.json",
    "tauri":          "tauri",
    "check:python":   "python3 -m py_compile boite-bridge.py boite-app.py boite-push.py",
    "check:rust":     "cd src-tauri && cargo check",
    "check:rust:mac-app-store": "cd src-tauri && cargo check --features mac-app-store",
    "test:rust":      "cd src-tauri && cargo test",
    "verify":         "npm run check:python && npm run check:rust"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
