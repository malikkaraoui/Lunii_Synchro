# Dépendances Python pour boite-bridge.py (sidecar de génération stories)
# L'interface graphique est désormais assurée par Tauri (Rust) — PySide6 supprimé.

# Requis par StoryBox.QT (device API + crypto)
PySide6-Essentials
xxtea
pycryptodome
requests

# Génération audio / couvertures
Pillow
mutagen
ffmpeg-python

# Utilitaires StoryBox.QT
unidecode
py7zr
